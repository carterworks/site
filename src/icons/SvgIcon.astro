---
const props = Astro.props;
const { viewBox = "0 0 24 24", verticalAlign = "bottom" } = props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  width="1em"
  height="1em"
  viewBox={viewBox}
  style={`vertical-align: ${verticalAlign}`}
  {...props}
>
  <slot />
</svg>
