{
  "name": "site",
  "version": "6.0.0",
  "private": true,
  "module": "src/index.astro",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/carterworks/site.git"
  },
  "scripts": {
    "dev": "wrangler types && DEV=true astro dev",
    "start": "wrangler types && DEV=true astro dev",
    "build": "wrangler types && astro build",
    "preview": "wrangler types && astro preview",
    "cf-typegen": "wrangler types",
    "format": "prettier --write .",
    "deploy": "wrangler types && astro build && wrangler pages deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251011.0",
    "astro": "5.14.5",
    "bun-types": "^1.3.0",
    "prettier": "^3.6.2",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.43.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "3.6.0",
    "@tailwindcss/vite": "^4.1.14",
    "tailwindcss": "^4.1.14"
  },
  "trustedDependencies": [
    "esbuild",
    "sharp",
    "workerd"
  ]
}
